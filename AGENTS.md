# 작업 기록과 커밋 규칙

- 작업 내용은 Markdown으로 기록. 목적, 변경 사항, 확인 근거, 검증 결과, 한계, 다음 작업 포함.
- 실제 AWS 계정·자원 ID, 접속 주소, 정책 본문, 보안 설정 등은 Git에서 제외된 `docs/private/`에 기록.
- 공개 가능한 작업 방법과 설계 근거는 `docs/`에 기록. 로컬 전용 파일로 향하는 링크를 공개 문서에 추가하지 않음.
- 커밋은 독립적으로 설명 가능한 작업 단위로 작성.
- 작업 브랜치에서 개발·검증 후 PR 생성. main 직접 푸시 및 자동 머지 금지.
- 머지 후 새 plan을 생성·검토하고 사용자가 직접 apply. 저장된 이전 브랜치 plan을 그대로 적용하지 않음.
- 모든 커밋 메시지에서 `Co-authored-by` 등 공동 작성자 문구 제외.
- 커밋 전에 공개 대상 파일과 제외 규칙 확인. 원본 응답, 실제 tfvars, state, plan은 커밋하지 않음.
